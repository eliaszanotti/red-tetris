{
    "name": "red-tetris",
    "private": true,
    "scripts": {
        "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter app dev\"",
        "dev:server": "pnpm --filter server dev",
        "dev:client": "pnpm --filter app dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
