{
	"name": "red-tetris",
	"private": true,
	"scripts": {
		"dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter vite dev\"",
		"dev:server": "pnpm --filter server dev",
		"dev:client": "pnpm --filter vite dev"
	},
	"devDependencies": {
		"concurrently": "^9.1.2"
	}
}
